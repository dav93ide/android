<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ColoredButton_01" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#0AC</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textSize">50px</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:topLeftRadius">50px</item>
        <item name="android:topRightRadius">50px</item>
        <item name="android:bottomLeftRadius">50px</item>
        <item name="android:bottomRightRadius">50px</item>
    </style>

</resources>
